# PostgreSQL connection
POSTGRES_USER=stripes
POSTGRES_PASSWORD=stripes
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=stripes_rag

# Embedding model
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DEVICE=cpu
EMBEDDING_BATCH_SIZE=64

# Optional: TEI server URL (omit for local sentence-transformers)
# EMBEDDING_SERVER_URL=http://localhost:8080

# Chunking
CHUNK_MAX_TOKENS=512

# Indexing
INDEX_BATCH_SIZE=128
MAX_FILE_SIZE_MB=50

# Vector index type: "hnsw" (high recall, default) or "ivfflat" (faster build)
VECTOR_INDEX_TYPE=hnsw
